const chalk = require('chalk')

console.log("Digite uma senha de 8 caracteres no mínimo")
let password = "123@d"

    if(password.charAt >= 8)  console.log(chalk.green("Senha criada!", res))
        else{console.log(chalk.red("Senha criada!", res))}
    
