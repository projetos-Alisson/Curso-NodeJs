const os = require("os"); //Extracts Operational System informations 

console.log(os.cpus()); //How many cpus

console.log(os.freemem()); //How free memory

console.log(os.homedir()); //Which directory

console.log(os.type()); //Which operational system? Linux, Windows
